/**
 * PizzaTier Dashboard (admin home) script.
 *
 * The Layer Manager tabs and the Tips & Tricks rotator were removed from the
 * dashboard, so there is currently no behaviour to bind here. This file is kept
 * as a stable, enqueued entry point for any future dashboard interactivity.
 */
( function () {
	'use strict';
	// Intentionally empty — no dynamic dashboard widgets at present.
} )();
